{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://raw.githubusercontent.com/channels-org/channels/master/schema/v0.1/change.json#",
  "title": "channel change",
  "description": "JSON Schema for the channel change",

  "type": "object",
  "required": ["id", "ts", "patch"],
  "additionalProperties": false,
  "properties": {
    "id": {"$ref": "defs.json#seqId"},
    "ts": {"$ref": "defs.json#timestamp"},
    "patch": {"$ref": "patch.json"}
  }
}
